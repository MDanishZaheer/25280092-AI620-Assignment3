Run the backend from the `Part-2` folder with: `uvicorn backend:app --reload`
Run the frontend from the `Part-2` folder with: `streamlit run frontend.py`
Open the FastAPI docs at: `http://127.0.0.1:8000/docs`
Open the Streamlit app at: `http://localhost:8501`
Start the backend first, then run the frontend in a second terminal.
